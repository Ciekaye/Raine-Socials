/** @type {import('next').NextConfig} */
const nextConfig = {
  // Placeholder art is served as SVG via SmartImage (unoptimized). Real photos
  // (jpg/png/webp) go through next/image optimization normally — no extra config needed.
};

export default nextConfig;
